name: task_management_redux
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.0.0+4
  path_provider: ^2.0.7
  built_collection: ^5.1.1
  built_value: ^8.1.3
  flutter_redux: ^0.8.2
  redux_epics: ^0.15.0
  rxdart: ^0.26.0
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  built_value_generator: ^8.1.3
  build_runner: ^2.1.5
  mockito: ^5.0.16
  flutter_lints: ^1.0.0



flutter:

  uses-material-design: true

